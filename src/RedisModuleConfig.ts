/**
 * Configuration for redis module.
 */
export interface RedisModuleConfig {

    /**
     * Redis host, connection with which will be established.
     */
    host: string;

    /**
     * Redis port.
     */
    port: number;

}
